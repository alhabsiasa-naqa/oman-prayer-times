# دليل استخدام ملف الإعدادات
# Settings File Guide

## 📋 نظرة عامة

يتيح لك ملف `settings.txt` التحكم في إعدادات أوقات الصلاة من GitHub مباشرة. عند تعديل هذا الملف، ستنعكس التغييرات على جميع الأجهزة التي تفتح الموقع (الكمبيوتر، الجوال، تطبيق Android).

> **ملاحظة:** تم إلغاء لوحة التحكم (admin.html) حيث أصبح التحكم الكامل عبر ملف `settings.txt` على GitHub.

---

## 🎯 كيفية العمل

```
1. تعديل settings.txt في GitHub
2. الموقع يقرأ الملف عند التحميل
3. تطبيق الإعدادات على جميع الأجهزة
```

---

## 📝 محتوى الملف

### **فارق التاريخ الهجري**
```
hijriOffset=0
```
- القيم الموجبة تضيف أيام: `hijriOffset=1` (يضيف يوم واحد)
- القيم السالبة تطرح أيام: `hijriOffset=-1` (يطرح يوم واحد)
- النطاق المسموح: `-5` إلى `+5`

**مثال:**
```
hijriOffset=1
```
إذا كان التاريخ الهجري يظهر "1 رجب"، سيصبح "2 رجب"

---

### **فارق التوقيت العام**
```
timeOffset=0
```
- يطبق على جميع الصلوات
- بالدقائق
- القيم الموجبة تضيف وقت، السالبة تطرح

**مثال:**
```
timeOffset=5
```
جميع أوقات الصلاة ستتأخر 5 دقائق

---

### **فوارق أوقات الصلاة الفردية**
```
fajrOffset=0
sunriseOffset=0
dhuhrOffset=0
asrOffset=0
maghribOffset=0
ishaOffset=0
```

**ملاحظة مهمة:** إذا تم تعيين فارق فردي لصلاة معينة، سيتم استخدامه بدلاً من `timeOffset` العام.

**مثال:**
```
timeOffset=5
dhuhrOffset=-3
```
- الفجر، الشروق، العصر، المغرب، العشاء: +5 دقائق
- الظهر فقط: -3 دقائق

---

### **اسم المسجد أو الموقع**
```
mosqueName=
```

**مثال:**
```
mosqueName=مسجد السلطان قابوس الأكبر
```

---

## 🔧 خطوات التعديل

### **الطريقة 1: عبر واجهة GitHub (الأسهل)**

1. افتح المشروع: https://github.com/alhabsiasa-naqa/oman-prayer-times
2. اضغط على ملف `settings.txt`
3. اضغط على أيقونة القلم (Edit) ✏️
4. عدّل القيم المطلوبة
5. اضغط **Commit changes** في الأسفل
6. انتظر 1-2 دقيقة
7. حدّث الموقع (F5)

### **الطريقة 2: عبر Git**

```bash
# تعديل الملف محلياً
nano settings.txt

# رفع التغييرات
git add settings.txt
git commit -m "تحديث إعدادات أوقات الصلاة"
git push
```

---

## 📱 للاستخدام مع تطبيق Android

الموقع سيقرأ `settings.txt` تلقائياً عند فتح التطبيق. لا حاجة لأي إعدادات إضافية في التطبيق.

**في كود Android WebView:**
```java
webView.loadUrl("https://alhabsiasa-naqa.github.io/oman-prayer-times/");
// الإعدادات ستُطبق تلقائياً من settings.txt
```

---

## 🎯 أمثلة عملية

### **مثال 1: تعديل التاريخ الهجري فقط**
```
hijriOffset=1
timeOffset=0
fajrOffset=0
sunriseOffset=0
dhuhrOffset=0
asrOffset=0
maghribOffset=0
ishaOffset=0
mosqueName=
```

### **مثال 2: تأخير جميع الأوقات 10 دقائق**
```
hijriOffset=0
timeOffset=10
fajrOffset=0
sunriseOffset=0
dhuhrOffset=0
asrOffset=0
maghribOffset=0
ishaOffset=0
mosqueName=
```

### **مثال 3: تعديلات مخصصة لكل صلاة**
```
hijriOffset=1
timeOffset=0
fajrOffset=5
sunriseOffset=0
dhuhrOffset=-3
asrOffset=2
maghribOffset=0
ishaOffset=7
mosqueName=مسجد السلطان قابوس الأكبر
```

---

## ⚠️ ملاحظات مهمة

1. **السطور التي تبدأ بـ `#` تُعتبر تعليقات** ويتم تجاهلها
2. **اترك القيمة فارغة** لاستخدام الإعدادات الافتراضية
3. **لا تحذف الأسطر** - فقط عدّل القيم
4. **الأولوية:** GitHub settings.txt > localStorage
5. **التحديث:** قد يستغرق 1-2 دقيقة بعد الرفع على GitHub

---

## 🔍 استكشاف الأخطاء

### **التغييرات لا تظهر؟**
1. تأكد من حفظ الملف في GitHub (Commit changes)
2. انتظر 2-3 دقائق
3. حدّث الصفحة (Ctrl+F5 أو Cmd+Shift+R)
4. افتح Console في المتصفح (F12) وابحث عن:
   ```
   Settings loaded from GitHub: {...}
   ```

### **الإعدادات لا تُطبق؟**
- تحقق من صحة الصيغة (key=value)
- تأكد من عدم وجود مسافات زائدة
- تأكد من أن القيم أرقام صحيحة

---

## 📞 الدعم

إذا واجهت أي مشكلة، تحقق من:
1. Console في المتصفح (F12)
2. تأكد من وجود الملف في المسار الصحيح
3. تحقق من صحة التنسيق

---

**تم إنشاء هذا الدليل لمشروع أوقات الصلاة - سلطنة عُمان** 🕌
